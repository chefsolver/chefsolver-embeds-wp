# assets/

Plugin assets:

- Future WordPress.org directory art (`icon-*.png`, `banner-*.png`, `screenshot-*.png`) — these sync to the SVN `assets/` directory after approval (see `docs/release-model.md`).
- Local editor assets (block editor JS/CSS) as features land. No remote assets, ever.
